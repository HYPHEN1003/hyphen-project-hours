# 画面設計書（HYPHEN版 案件別工数管理システム）

- **ドキュメント種別**: UI_DESIGN.md（Step 4 / 全5点中の4点目）
- **前提**: REQUIREMENTS.md / SHEETS_SCHEMA.md / API_DESIGN.md ／ 正本マニュアル `案件別工数管理.txt`
- **作成日**: 2026-06-04
- **ステータス**: ドラフト（中村レビュー待ち）

---

## 0. 設計方針

1. **元アプリのサイドメニュー構成を踏襲**（6グループ／全15画面）。
2. **静的 SPA**（HTML/CSS/Vanilla JS）。ビルドツールなしで Netlify に直接デプロイできる軽量構成。フレームワークは使わず、小さなルーター＋テンプレート描画で構成（依存は Chart.js のみ）。
3. **HYPHEN ブランドを差し替え可能に**: 色・ロゴ・社名は CSS カスタムプロパティ（`:root` 変数）と 1ファイルの設定で集中管理。後から HYPHEN カラーに一括変更できる。
4. **色の意味を全画面で統一**: 🔴赤=警告/超過/赤字、🟡黄=注意、🟢緑=良好。
5. **レスポンシブ**: PC（サイドバー常時表示）／タブレット・スマホ（ハンバーガー `≡` で開閉）。
6. **日本語 UI・日本語エラー**。アクセシビリティ（コントラスト比・キーボード操作）に配慮。
7. **API は API_DESIGN.md のエンベロープを使用**。フロントは表示専任、計算結果はサーバ値を表示。

---

## 1. デザインシステム

### 1.1 カラートークン（CSS 変数・差し替え前提）
```css
:root {
  /* ブランド（HYPHEN差し替えポイント） */
  --brand-primary:   #2563eb;   /* メインカラー（仮）→ HYPHEN指定色へ */
  --brand-primary-d: #1e40af;
  --brand-accent:    #0ea5e9;
  --brand-logo-url:  url("./assets/logo.svg");

  /* セマンティック（意味は固定・色値のみ調整可） */
  --c-danger:  #dc2626;  /* 赤: 警告/超過/赤字 */
  --c-warn:    #d97706;  /* 黄(琥珀): 注意 */
  --c-success: #16a34a;  /* 緑: 良好 */
  --c-info:    #0284c7;

  /* 背景・面・文字 */
  --bg:        #f5f7fa;
  --surface:   #ffffff;
  --border:    #e2e8f0;
  --text:      #1e293b;
  --text-mute: #64748b;

  /* レイアウト */
  --sidebar-w: 240px;
  --header-h:  56px;
  --radius:    10px;
  --shadow:    0 1px 3px rgba(0,0,0,.08);
  --font: "Noto Sans JP", "Hiragino Sans", "Meiryo", system-ui, sans-serif;
}
```
- **ダークモードは MVP 範囲外**（変数構成上、後から `[data-theme=dark]` で追加可能）。

### 1.2 ステータス→色のマッピング（バッジ）
| 文脈 | 値 | 色 |
|---|---|---|
| 案件アラート | 超過 | 🔴 danger |
| 案件アラート | 警告 | 🟡 warn |
| 案件アラート | 正常 | 灰/success |
| 粗利益率 | ≥50% | 🟢 success |
| 粗利益率 | <30% | 🔴 danger |
| 稼働率 | ≥100% | 🔴 danger（過負荷） |
| 稼働率 | <70% | 🟡 warn（余裕） |
| 収益性評価 | 高 | 🟢 / 中 灰 / 低 🟡 / 赤字 🔴 |
| 予測アラート | 過負荷🔴 / 適正🟢 / 余裕🟡 |
| 案件ステータス | 進行中=info / 完了=success / 中止=灰 / 請求済・入金済=brand |
| 請求ステータス | 下書き=灰 / 発行済=info / 入金済=success / 一部入金=warn / 遅延=danger |
| 承認状態 | 承認=success / 申請中=warn / 却下=danger |

### 1.3 共通コンポーネント
- **ボタン**: primary / secondary / danger / ghost。アイコン+ラベル。
- **バッジ/チップ**: ステータス表示（§1.2 の色）。
- **テーブル**: ソート可ヘッダ、行ホバー、行クリックで編集モーダル、ゼブラ、件数表示、空状態プレースホルダ。
- **モーダル**: フォーム用（中央・オーバーレイ、Esc/×で閉、未保存時に確認）。
- **タブ**: 工数入力（4タブ）等で使用。
- **フィルタバー**: セレクト＋日付範囲＋検索、右寄せ「+新規」ボタン。
- **トースト**: 保存成功/エラー通知（右上、数秒で消える）。
- **ローディング**: ボタン内スピナー、テーブルのスケルトン。
- **数値表示**: 3桁区切り、金額は「¥」、率は「%」、時間は「h」。
- **フォーム部品**: select（マスタ連動）、日付/時刻ピッカー、数値入力（min/step=最小入力単位）、必須マーク。

---

## 2. 全体レイアウト

```
┌────────────────────────────────────────────────────────┐
│ [≡] HYPHEN ロゴ        案件別工数管理システム      2026/06/04 (header-h)│
├──────────────┬─────────────────────────────────────────┤
│ サイドバー     │  メインエリア                              │
│ (sidebar-w)   │  ┌─ ページタイトル ──────── [操作ボタン] ─┐ │
│  [メイン]      │  │  パンくず / 月選択 など                 │ │
│   ダッシュボード│  ├─────────────────────────────────────┤ │
│   工数入力     │  │                                       │ │
│   案件管理     │  │   コンテンツ（カード/テーブル/グラフ） │ │
│  [マスタ]      │  │                                       │ │
│   顧客マスタ   │  │                                       │ │
│   …           │  └─────────────────────────────────────┘ │
│  [システム]    │                                            │
│   設定         │                                            │
└──────────────┴─────────────────────────────────────────┘
```

### 2.1 サイドバー（元アプリ踏襲）
```
[メイン]      ダッシュボード / 工数入力 / 案件管理
[マスタ]      顧客マスタ / スタッフマスタ / 案件種別マスタ
[取引]        経費管理 / 請求管理
[レポート]    案件別 / スタッフ別 / 月次 / 顧客別
[分析]        見積精度分析 / 稼働予測
[システム]    設定
```
- 現在ページをハイライト。グループ見出しは非クリック。
- 下部に HYPHEN ブランドフッタ（社名・バージョン）。

### 2.2 ヘッダ
- 左: `≡`（モバイルのみ）＋ロゴ＋システム名。
- 右: 当日日付（サーバ日付 `serverTime`）。将来: ユーザー名/ログアウト。

### 2.3 ルーティング
- ハッシュルーター（`#/dashboard`, `#/time`, `#/projects` …）。リロード耐性・Netlify で追加設定不要。
- 起動時に `bootstrap` を1回呼び、マスタ・設定をメモリキャッシュ（セレクト即時描画）。

---

## 3. 画面別設計（全15画面）

各画面: ［レイアウト］／［主要コンポーネント］／［モーダル/操作］／［API］／［色］。

---

### 3-1. ダッシュボード `#/dashboard`
- **レイアウト**: 上段=KPIカード4枚、中段=アラート一覧、下段=月次サマリーグラフ。
- **KPIカード**: ①進行中案件数 / 全案件数　②今月の総稼働時間(h)　③今月売上(¥, 直近月次より)　④警告+超過件数。
- **アラート一覧**: 表（案件名・顧客・消化率バー・アラートバッジ）。行クリック→案件編集へ。
- **グラフ**: 直近6ヶ月の「売上・粗利益」複合（棒=売上、折線=粗利益率）。Chart.js（§4）。
- **API**: `getDashboard`。
- **色**: 超過🔴/警告🟡。消化率バーは閾値で色変化。

### 3-2. 工数入力 `#/time`（4タブ）
共通: 上部タブ（個別 / 一括 / タイマー / 履歴）。

**(A) 個別入力**
- 左: 入力フォーム（記録日[既定今日]・スタッフ・案件・作業項目・開始/終了時刻・作業時間(分)・請求区分[既定:作業項目の既定値]・作業内容）。
- 右: 当日の自分の入力一覧（即時反映・合計時間表示）。
- 時刻入力↔分入力は連動（時刻入れると分が自動算出、グレー表示。分直接入力も可）。
- 「保存」でトースト＋一覧追加。
- **API**: `createTimeEntry`。

**(B) 一括入力**
- 上: スタッフ・記録日を選択。
- 下: 行追加式テーブル（案件・作業項目・作業時間(分)・請求区分・作業内容、行末に削除）。「+行追加」。
- 「一括登録」→ エラー行を赤ハイライト＋メッセージ、成功で全行クリア。
- **API**: `bulkCreateTimeEntries`。

**(C) タイマー** ⏱（重点 UI）
```
┌─────────────── タイマー ───────────────┐
│  案件:  [ 山田製作所 法人決算申告 ▼ ]      │
│  作業項目: [ 申告書作成 ▼ ]               │
│                                          │
│            ●REC   01:23:45               │  ← 計測中は赤ドット点滅・大型表示
│                                          │
│   [ ▶ 開始 ]  [ ⏸ 一時停止 ]  [ ■ 停止・保存 ] │
│   請求区分: ( ●請求対象 ○内部 ○サービス )  │
│   作業内容: [____________________]        │
└──────────────────────────────────────┘
```
- 状態: 待機 / 計測中（赤●点滅・経過時間 `HH:mm:ss` 1秒更新）/ 一時停止（黄表示・累積保持）。
- 「開始」前に案件・作業項目必須（未選択ならボタン無効）。
- 「停止・保存」: 累積秒→分換算→最小入力単位で丸め→確認（丸め結果を提示）→保存。
- **計測状態を localStorage に保持**（開始時刻・累積・一時停止区間）。リロード/タブ復帰で復元。離脱警告（`beforeunload`）。
- **API**: `saveTimerEntry`。
- **色**: 計測中=danger アクセント、一時停止=warn。

**(D) 履歴**
- フィルタバー（スタッフ・案件・日付範囲・請求区分）。
- テーブル（記録日・スタッフ・案件・作業項目・時間・請求区分・内容）。行クリック→編集モーダル（削除可）。
- 下部: 抽出結果の合計時間・原価・請求金額。
- **API**: `listTimeEntries` / `updateTimeEntry` / `deleteTimeEntry`。

### 3-3. 案件管理 `#/projects`
- **レイアウト**: フィルタバー（ステータス[既定:進行中]・顧客・主担当・検索）＋「+新規」。テーブル。
- **テーブル列**: 案件名・顧客・種別・契約形態・主担当・契約金額・見積工数・実績/消化率(バー)・納期・ステータス・アラート。
- **モーダル（新規/編集）**: REQUIREMENTS §5-3 の項目。案件種別選択で標準工数/単価を初期提案。必須=案件名・顧客。
- **API**: `listProjects` / `getProject` / `createProject` / `updateProject` / `deleteProject`。
- **色**: 消化率バー（80%🟡/100%🔴）、ステータスバッジ。

### 3-4. 顧客マスタ `#/customers`
- フィルタ（ランク・ステータス・区分・検索）＋「+新規」。テーブル（顧客名・カナ・区分・業種・ランク・支払条件・締日・ステータス）。
- モーダル: REQUIREMENTS §5-4。email/電話形式チェック。
- **API**: `listCustomers` ほか CRUD。

### 3-5. スタッフマスタ `#/staff`
- テーブル（氏名・役職・職種・原価単価・請求単価・標準稼働h・稼働率目標・ステータス）。
- モーダル: 単価・標準稼働・稼働率目標。請求単価<原価単価は警告表示（保存は可）。
- **API**: `listStaff` ほか CRUD。

### 3-6. 案件種別マスタ `#/project-types`
- テーブル（種別名・カテゴリ大・標準工数・標準単価・難易度[★表示]）。
- モーダル: 難易度は1〜5の星/セレクト。
- **API**: `listProjectTypes` ほか CRUD。
- （補足）作業項目マスタは設定内のサブ画面 or このグループに小さく併設（§3-15 参照）。

### 3-7. 経費管理 `#/expenses`
- フィルタ（案件・スタッフ・経費種別・請求区分・承認状態・日付範囲）＋「+新規」。
- テーブル（発生日・スタッフ・案件・種別・金額・請求区分・領収書・承認状態・内容）。
- モーダル: REQUIREMENTS §5-7。承認状態は権限により変更（MVPは誰でも可）。一覧から「承認/却下」クイック操作。
- **API**: `listExpenses` / CRUD / `approveExpense`。
- **色**: 承認=🟢/申請中=🟡/却下=🔴。

### 3-8. 請求管理 `#/invoices`
- フィルタ（顧客・ステータス・請求日範囲）＋「+新規請求」。
- テーブル（請求番号・顧客・案件・請求日・合計・入金額・ステータス・支払期限）。期限超過未入金は行を🔴強調。
- **新規請求モーダル（重点）**:
  1. 顧客・案件・請求日・請求期間(FROM/TO) 入力。
  2. 「自動計算」ボタン→ `calculateInvoice` 呼び出し→ 基本報酬/タイムチャージ/実費/小計/消費税/合計を表示＋明細プレビュー表。
  3. 各金額は手動調整可（変更で合計再計算）。
  4. 支払期限（顧客支払条件から初期提案）・ステータス選択。
  5. 「保存」→ `createInvoice`（番号採番）。
- **入金登録**: 行から「入金登録」→ 金額入力→ `recordPayment`（ステータス自動判定）。
- **API**: `listInvoices` / `getInvoice` / `calculateInvoice` / `createInvoice` / `updateInvoice` / `recordPayment`。

### 3-9. 案件別レポート `#/report/projects`
- フィルタ（顧客・ステータス・粗利率しきい）＋ソート。
- テーブル（案件名・顧客・契約金額・実績工数・消化率・総原価・粗利益・粗利益率・アラート）。
- 粗利益率セルを色分け（≥50%🟢 / <30%🔴）、消化率バー、アラートバッジ。
- 上部に集計カード（合計契約額・合計粗利・平均粗利率）。
- **API**: `reportProjects`。

### 3-10. スタッフ別レポート `#/report/staff`
- 右上に**月選択**（YYYY-MM）。
- テーブル（氏名・役職・総稼働h・請求対象h・稼働率・達成度・担当案件数・売上貢献額）。
- 稼働率を色分け（≥100%🔴 / <70%🟡）。稼働率はバー表示。
- 補助グラフ: スタッフ別稼働率 横棒（§4）。
- **API**: `reportStaff?yearMonth=`。

### 3-11. 月次レポート `#/report/monthly`
- 期間選択（from/to、既定=直近12ヶ月）。
- KPIカード（当月売上・前月比・粗利益率・全体稼働率）。
- **グラフ**: 売上・粗利益の月次推移（複合棒＋折線）、粗利益率折線（§4）。
- テーブル（月・売上・前月比・総原価・粗利益・粗利益率・完了数・新規数・全体稼働率）。
- **API**: `reportMonthly?from=&to=`。

### 3-12. 顧客別レポート `#/report/customers`
- 月選択。テーブル（顧客名・ランク・案件数・売上・工数・粗利益・粗利益率・収益性評価）。
- 収益性評価バッジ（高🟢/中/低🟡/赤字🔴）。
- 補助グラフ: 顧客別売上 上位 横棒。
- **API**: `reportCustomers?yearMonth=`。

### 3-13. 見積精度分析 `#/analysis/estimate`
- 完了案件テーブル（案件名・種別・見積h・実績h・乖離率・精度評価・改善提案）。
- 乖離率は中央0の差分バー（+🔴右/−🔵左）。精度評価バッジ（高🟢/中/低🔴）。
- 上部に**種別別平均乖離率**サマリー（カード or 横棒グラフ。例「相続税申告 +30%」）。
- **API**: `analyzeEstimateAccuracy`。

### 3-14. 稼働予測 `#/analysis/forecast`
- 対象月トグル（今月 / 来月）。
- テーブル（スタッフ・稼働可能h・確定案件工数・予測稼働h・予測稼働率・空き時間・アラート）。
- 予測稼働率バー（100%🔴/70%🟡境界線）。アラートバッジ（過負荷🔴/適正🟢/余裕🟡）。
- 補助グラフ: スタッフ別 予測稼働率 横棒（目標線オーバーレイ）。
- **API**: `forecastCapacity?yearMonth=`。

### 3-15. 設定 `#/settings`
- **セクション1: 手動集計**（ボタン4つ＋稼働予測）: 案件別集計 / 月次集計 / スタッフ別集計 / 見積精度分析 / 稼働予測。押下で実行→完了トースト（更新件数・集計日時表示）。実行中はスピナー。
- **セクション2: システム設定**: 消費税率・最小入力単位(分)・各アラート閾値（消化率/稼働率/粗利率）・請求番号プレフィックス・アラート通知先メール。`getSettings`/`updateSettings`。
- **セクション3: 作業項目マスタ**（小規模 CRUD テーブル。§3-6 で触れた配置）。
- **セクション4: ブランド設定（任意）**: 社名・ロゴ・メインカラー（CSS変数を上書きするための値。MVPは表示のみ/将来編集）。
- **API**: `getSettings` / `updateSettings` / `run*`。

---

## 4. グラフ仕様（Chart.js）

| 画面 | グラフ | 種類 | データ |
|---|---|---|---|
| ダッシュボード | 直近6ヶ月サマリー | 複合（棒:売上＋折線:粗利益率） | `getDashboard.recentMonthly` |
| 月次レポート | 売上・粗利推移 | 複合（棒:売上,粗利益＋折線:粗利益率） | `reportMonthly` |
| スタッフ別レポート | 稼働率比較 | 横棒（目標線） | `reportStaff` |
| 顧客別レポート | 売上上位 | 横棒 | `reportCustomers` |
| 見積精度分析 | 種別別平均乖離率 | 横棒（0中心、±色分け） | `analyzeEstimateAccuracy` |
| 稼働予測 | 予測稼働率 | 横棒（100%/目標ライン） | `forecastCapacity` |

- **共通方針**: Chart.js v4 を CDN 読み込み。色はカラートークン参照（`getComputedStyle` で `--c-*` 取得）。
- レスポンシブ（`responsive:true, maintainAspectRatio:false`）、ツールチップ日本語＋3桁区切り＋単位。
- 閾値線は `annotation` 相当（プラグイン未使用なら横線データセットで代替）。
- 描画は集計シート由来の値のみ（フロントで再計算しない）。

---

## 5. モーダル共通仕様

- 中央配置・オーバーレイ（クリックで閉じる確認）、Esc で閉じる。
- ヘッダ（タイトル＋×）／ボディ（フォーム）／フッタ（キャンセル・保存）。
- フォーム: 必須に `*`、インラインバリデーション（blur時）、送信時に全体検証→サーバ `VALIDATION_ERROR` の `details[]` を該当フィールド下に表示。
- 保存中はボタンスピナー＆二重送信防止。
- 編集時は既存値プリフィル。未保存変更ありで閉じる時は確認ダイアログ。
- モバイルでは全画面シート化。

---

## 6. レスポンシブ方針

| ブレークポイント | 挙動 |
|---|---|
| `≥1024px`（PC） | サイドバー常時表示。テーブルは全列表示。 |
| `768–1023px`（タブレット） | サイドバーは `≡` 開閉（オーバーレイ）。テーブルは横スクロール。 |
| `<768px`（スマホ） | サイドバー off-canvas。テーブルは重要列のみ＋「詳細」展開、またはカード化。モーダルは全画面。グラフは高さ固定で縦積み。 |

- タッチ操作: ボタン・行のタップ領域を 44px 以上確保。
- 入力: 数値はモバイルで数値キーボード（`inputmode`）。
- KPIカードは grid で自動折返し（PC4列→スマホ1〜2列）。

---

## 7. 状態・エラー・空表示

- **ローディング**: 初回 `bootstrap` は全画面スプラッシュ、以降はセクション単位スケルトン。
- **空状態**: 「データがありません。＋新規 から登録してください」＋アイコン。
- **エラー**: API `ok:false` → エラーコード別の日本語メッセージをトースト＋必要に応じ再試行ボタン。`LOCK_TIMEOUT` は「混み合っています。再実行してください」。
- **オフライン/通信失敗**: 「通信に失敗しました」トースト。タイマー計測はローカル継続。
- **権限（将来）**: 認証導入後、メニュー/操作の出し分け口を用意（MVPは全機能表示）。

---

## 8. ブランド差し替え手順（HYPHEN 対応）

1. `assets/logo.svg` を HYPHEN ロゴへ差し替え。
2. `css/tokens.css` の `--brand-*` を HYPHEN カラーに変更。
3. `config.js` の `APP_NAME` / `COMPANY_NAME` / `FOOTER_TEXT` を変更。
4. セマンティックカラー（danger/warn/success）は意味固定のため原則変更しない（必要時のみ色値調整）。
→ HTML/JS のロジックは無変更でブランド反映完了。

---

## 9. フロントエンド ファイル構成（IMPLEMENTATION_PLAN.md で確定）
```
frontend/
  index.html              … 単一HTML（SPAシェル: サイドバー/ヘッダ/コンテンツ枠）
  css/
    tokens.css            … カラー・レイアウト変数（ブランド差し替え点）
    base.css              … リセット・タイポ・共通要素
    components.css        … ボタン/テーブル/モーダル/バッジ/タブ/トースト
    layout.css            … サイドバー/ヘッダ/レスポンシブ
  js/
    config.js             … API_BASE_URL・社名・定数
    api.js                … fetchラッパ（エンベロープ処理・エラー整形）
    router.js             … ハッシュルーター
    store.js              … bootstrapキャッシュ（マスタ/設定）
    components/           … modal.js / table.js / toast.js / chart.js(ラッパ) / form.js
    pages/                … dashboard.js / time.js / projects.js / customers.js /
                            staff.js / projectTypes.js / expenses.js / invoices.js /
                            reportProjects.js / reportStaff.js / reportMonthly.js /
                            reportCustomers.js / estimate.js / forecast.js / settings.js
    utils/                … format.js(金額/日付/率) / validate.js / timer.js(タイマー状態)
  assets/
    logo.svg
```

---

## 10. 中村レビュー確認事項

| 項目 | 本書の方針 | 要確認 |
|---|---|---|
| メインカラー（仮 #2563eb） | 後で HYPHEN 指定色へ差し替え | HYPHEN ブランドカラーの値 |
| ロゴ | `assets/logo.svg` 差し替え | ロゴデータの支給 |
| フォント | Noto Sans JP（Google Fonts） | 指定フォントの有無 |
| ダークモード | MVP 範囲外（拡張可） | 要否 |
| 作業項目マスタの配置 | 設定画面内のサブ CRUD | マスタグループ独立要否 |
| グラフ注釈線 | 標準機能で代替（プラグイン不使用） | annotation プラグイン採用可否 |

---

（以上 Step 4。中村のフィードバック確認後、Step 5: IMPLEMENTATION_PLAN.md に進む。）
