# 案件別工数管理システム（HYPHEN版）要件定義書

- **ドキュメント種別**: REQUIREMENTS.md（Step 1 / 全5点中の1点目）
- **対象**: HYPHEN版「案件別工数管理システム」サンプル（他社紹介・ポートフォリオ用）
- **元アプリ**: https://ankenbetu-kousu.netlify.app/ ／ 公式マニュアル `案件別工数管理.txt`
- **作成日**: 2026-06-04
- **ステータス**: ドラフト（中村レビュー待ち）

---

## 0. このドキュメントの位置づけ

本書は元アプリの公式マニュアル（`案件別工数管理.txt`）を正本として、機能を漏れなく抽出・明文化したものである。
後続の設計書（SHEETS_SCHEMA / API_DESIGN / UI_DESIGN / IMPLEMENTATION_PLAN）は本書を起点とする。

- **再現方針**: 元アプリの機能を 100% 再現することを目標とする。
- **未確定事項**: マニュアルに記載がなく合理的に推定した項目は「【推定】」と明記する。中村のレビューで確定する。
- **設計の通底方針**: 将来の Supabase 移行を見据え、データアクセスは DataRepository 層を必ず経由する（本書では業務要件のみ扱い、実装方式は API_DESIGN.md で詳細化する）。

---

## 1. システム概要

### 1.1 目的
士業・コンサルティング・制作会社など「案件単位で仕事を進める企業」向けに、案件ごとの工数（作業時間）・収益・経費・請求を一元管理し、収益性とスタッフ稼働を可視化する。

### 1.2 提供価値（元マニュアル 6章より）
1. 案件の赤字を未然に防止（消化率アラート）
2. 正確な収益管理（案件別の粗利益・粗利率を自動計算）
3. スタッフの適正配置（稼働率・稼働予測の可視化）
4. 見積もり精度の継続的向上（見積 vs 実績の蓄積分析）
5. 経営判断のスピードアップ（月次・顧客別レポート自動集計）
6. 請求漏れの防止（工数・経費からの請求金額自動計算）

### 1.3 技術スタック（確定済み）
| 層 | 技術 |
|---|---|
| フロントエンド | 静的 HTML/CSS/JavaScript（Netlify ホスト） |
| バックエンド | Google Apps Script Web アプリ（doGet/doPost） |
| データベース | Google スプレッドシート（SpreadsheetApp） |
| 認証 | なし（MVP 段階） |
| グラフ描画 | Chart.js |

### 1.4 想定ユーザー・利用形態
- 複数スタッフが同時に Web アプリへアクセスして工数入力・データ確認を行う（FAQ より）。
- PC・スマートフォン両対応（レスポンシブ、モバイルはハンバーガーメニュー）。
- スプレッドシート直接編集は非推奨（整合性が崩れるため）。Web アプリ経由を原則とする。

---

## 2. 画面一覧（全15画面）

サイドバーは元アプリの構成を踏襲し、6グループに分類する。

| # | グループ | 画面名 | 主目的 |
|---|---|---|---|
| 1 | メイン | ダッシュボード | 全体概況・アラート確認 |
| 2 | メイン | 工数入力 | 日々の作業時間記録（個別/一括/タイマー/履歴） |
| 3 | メイン | 案件管理 | 案件（仕事）の登録・管理 |
| 4 | マスタ | 顧客マスタ | 取引先情報の管理 |
| 5 | マスタ | スタッフマスタ | 自社メンバー・単価の管理 |
| 6 | マスタ | 案件種別マスタ | 案件種別テンプレートの管理 |
| 7 | 取引 | 経費管理 | 案件関連経費の記録 |
| 8 | 取引 | 請求管理 | 請求書データの作成・管理 |
| 9 | レポート | 案件別レポート | 案件ごとの収益性一覧 |
| 10 | レポート | スタッフ別レポート | スタッフ別稼働状況（月別） |
| 11 | レポート | 月次レポート | 月次経営数値の推移 |
| 12 | レポート | 顧客別レポート | 顧客ごとの収益性（月別） |
| 13 | 分析 | 見積精度分析 | 見積 vs 実績の乖離分析 |
| 14 | 分析 | 稼働予測 | 今月・来月の稼働見込み |
| 15 | システム | 設定 | システム設定・手動集計の実行 |

各画面の上部には「現在のページ名」と「日付」を表示する（元マニュアル 2章）。

---

## 3. 共通仕様

### 3.1 ID 命名規則（確定済み）
将来 PostgreSQL の主キーとしてそのまま使える形で統一する。

| 種別 | 接頭辞 | 例 | 採番方式 |
|---|---|---|---|
| 案件 | `PJT-` | PJT-001 | 連番（ゼロ埋め3桁） |
| スタッフ | `STF-` | STF-001 | 連番（ゼロ埋め3桁） |
| 顧客 | `CUS-` | CUS-001 | 連番（ゼロ埋め3桁） |
| 案件種別 | `KND-` | KND-001 | 連番（ゼロ埋め3桁） |
| 工数 | `KOSU-` | KOSU-001 | 連番（ゼロ埋め3桁。将来桁拡張前提） |
| 経費 | `KEIHI-` | KEIHI-001 | 連番（ゼロ埋め3桁） |
| 請求書 | `INV-` | INV-202602-001 | `INV-YYYYMM-連番3桁`（月内リセット） |

- 工数レコードは年間1万行超を想定。`KOSU-` の連番桁数は将来拡張可能とする（採番ロジックは桁数固定に依存しない実装とする → API_DESIGN.md）。
- 採番は LockService による排他制御下で行い、採番重複を防ぐ（API_DESIGN.md で詳述）。

### 3.2 色の意味（全画面共通）
| 色 | 意味 |
|---|---|
| 🔴 赤 | 警告・超過・赤字（即対応が必要） |
| 🟡 黄 | 注意（閾値接近） |
| 🟢 緑 | 良好（収益性が高い等） |

### 3.3 日付・時刻・数値の表記
- 日付: `YYYY-MM-DD`（内部保持）。UI 表示は `YYYY/MM/DD` 【推定】。
- 月: `YYYY-MM`（レポートの対象月選択・請求番号等）。
- 時刻: `HH:mm`（24時間表記）。
- 作業時間: 内部は「分」で保持し、時間換算（分 ÷ 60）を計算する。
- 金額: 円・整数（小数なし）。表示は3桁区切り。
- 最小入力単位: 設定の「最小入力単位（分）」（デフォルト15分）に従う。

### 3.4 多言語・メッセージ
- UI・エラーメッセージはすべて日本語。
- HYPHEN 独自ロゴ・色味は後から差し替え可能な構造とする（UI_DESIGN.md で CSS 変数化）。

### 3.5 同時編集
- 複数ユーザー同時アクセス前提。書き込み系処理は LockService で排他制御する（API_DESIGN.md）。

### 3.6 論理削除 / 物理削除【推定】
- マスタ（顧客・スタッフ・案件種別・案件）は「ステータス」で休止・解約・中止を表現でき、参照整合性を保つため**物理削除は行わず論理的に無効化**する方針を推奨。
- 工数・経費レコードは履歴タブからの編集・削除が可能（FAQ より）。削除は物理削除とするか論理削除（削除フラグ）とするかは中村確認事項 → 集計の整合性のため**論理削除（削除フラグ）を推奨**。

---

## 4. 自動計算ロジック（最重要・正本）

すべての計算は GAS 側（DataRepository 層／集計関数）で行うことを基本とし、フロントは表示のみ。
集計結果は生データシートを更新せず、集計用シートへ別保存する（SHEETS_SCHEMA.md）。

### 4.1 工数レコード単位の計算
| 項目 | 計算式 | 備考 |
|---|---|---|
| 作業時間（時間換算） | `作業時間(分) ÷ 60` | 時刻入力時は `(終了時刻 - 開始時刻)` を分換算 |
| 原価金額 | `スタッフ原価単価(円/時) × 作業時間(時間)` | スタッフマスタの「標準時給単価（原価）」を使用 |
| 請求金額 | `スタッフ請求単価(円/時) × 作業時間(時間)` | スタッフマスタの「標準請求単価（売価）」を使用。請求区分が「請求対象」のもののみ請求集計に算入 |

- **単価のスナップショット方針【推定・要確認】**: 単価は時期により改定されうるため、工数レコード登録時点のスタッフ単価を工数レコードへスナップショット保存することを推奨（後日の単価改定で過去の原価・請求金額が変動しないようにする）。Supabase 移行時の再計算容易性も向上。

### 4.2 案件単位の計算（案件別レポート／案件集計シート）
| 項目 | 計算式 | 備考 |
|---|---|---|
| 実績工数 | `当該案件の全工数レコードの作業時間(時間)合計` | |
| 工数消化率 | `実績工数 ÷ 見積工数` | 見積工数=0/未設定時はアラート対象外（0除算回避） |
| 総人件費（原価） | `当該案件の全工数レコードの原価金額合計` | |
| 経費合計（原価算入分） | `当該案件の経費のうち「自社負担」「含む」の合計` 【推定】 | 原価に含める経費の範囲は要確認（4.6参照） |
| 総原価 | `総人件費 + 経費（原価算入分）` | |
| 粗利益 | `契約金額 − 総原価` | |
| 粗利益率 | `粗利益 ÷ 契約金額` | 契約金額=0時は「—」表示 |

### 4.3 アラート判定（消化率）
| 条件 | 判定 | 色 |
|---|---|---|
| 消化率 ≥ 100% | 予算超過 | 🔴 赤 |
| 80% ≤ 消化率 < 100% | 警告 | 🟡 黄 |
| 消化率 < 80% | 正常 | （無色 / 🟢） |

- 閾値（80% / 100%）は設定シートの「各種アラート閾値」で変更可能とする。
- 予算超過時は設定の「アラート通知先メール」へ通知（任意機能。MVP では将来対応可）【推定】。

### 4.4 収益性評価（案件別レポートの色分け）
| 条件 | 評価 | 色 |
|---|---|---|
| 粗利益率 ≥ 50% | 収益性が高い | 🟢 緑 |
| 30% ≤ 粗利益率 < 50% | 通常 | （無色） |
| 粗利益率 < 30% | 要注意 | 🔴 赤 |

### 4.5 スタッフ単位の計算（スタッフ別レポート／スタッフ集計シート、月別）
| 項目 | 計算式 | 備考 |
|---|---|---|
| 総稼働時間 | `対象月・当該スタッフの全工数レコードの作業時間(時間)合計` | |
| 請求対象時間 | `そのうち請求区分=「請求対象」の作業時間合計` | |
| 稼働率 | `総稼働時間 ÷ 月間標準稼働時間` | 標準稼働時間=スタッフマスタ値（通常160h） |
| 稼働率達成度 | `稼働率 ÷ 目標稼働率` | 目標稼働率=スタッフマスタ値（通常0.80） |
| 担当案件数 | `対象月に工数を入力した案件のユニーク数` 【推定】 | 「主担当」基準か工数基準かは要確認 |
| 売上貢献額 | `請求対象時間 × 請求単価の合計（=請求金額合計）` 【推定】 | |

稼働率の色分け（スタッフ別レポート）:
| 条件 | 色 | 意味 |
|---|---|---|
| 稼働率 ≥ 100% | 🔴 赤 | 過負荷（業務量調整） |
| 稼働率 < 70% | 🟡 黄 | 余裕あり（割り当て検討） |
| 70% ≤ 稼働率 < 100% | （無色） | 適正 |

### 4.6 経費の請求／原価への算入ルール
経費の「請求区分」により扱いが分かれる。
| 請求区分 | 請求書への算入 | 案件原価への算入【推定】 |
|---|---|---|
| 実費請求 | ○（承認済みのみ請求書の実費金額に合算） | ×（顧客請求のため原価ではない） |
| 含む | ×（報酬に含まれるため別請求しない） | ○（コストとして原価算入） |
| 自社負担 | ×（請求しない） | ○（会社負担コストとして原価算入） |

- 請求書自動計算に含まれる条件: **「実費請求」かつ「承認状態=承認」**（マニュアル 4-7 注記）。
- 原価への算入範囲は中村確認事項（上表は推定）。

### 4.7 請求書の計算（請求管理）
請求書「自動計算」ボタン押下時、請求期間（FROM/TO）と顧客・案件を条件に以下を算出する。
| 項目 | 計算式 |
|---|---|
| 基本報酬 | 契約形態が「固定」「スポット」の場合、案件の契約金額 |
| タイムチャージ金額 | 請求期間内・請求対象工数の `Σ(作業時間 × 請求単価)`（=請求金額合計） |
| 実費金額 | 請求期間内・承認済み・請求区分=実費請求 の経費金額合計 |
| 小計 | `基本報酬 + タイムチャージ金額 + 実費金額` 【推定】 |
| 消費税 | `小計 × 消費税率`（設定値、デフォルト 0.10） |
| 合計請求額 | `小計 + 消費税` 【推定】 |

- 自動計算後、ユーザーが金額を手動調整可能（マニュアル 4-8 手順5）。
- 請求番号採番: `INV-YYYYMM-連番`（YYYYMM=請求日の年月、連番=その月内の通し3桁）。
- 契約形態と基本報酬の対応（タイムチャージ契約は基本報酬を0とし工数のみ請求等）は要確認【推定】。

### 4.8 月次集計（月次レポート／月次サマリーシート）
| 項目 | 計算式 |
|---|---|
| 売上合計 | 対象月の請求金額合計（または完了/請求案件ベース）【推定・要定義】 |
| 前月比 | `当月売上 ÷ 前月売上 − 1` |
| 総原価 | 対象月の人件費（工数原価）合計 + 経費（原価算入分）合計 |
| 粗利益 | `売上合計 − 総原価` |
| 粗利益率 | `粗利益 ÷ 売上合計` |
| 完了案件数 | 対象月にステータスが「完了」になった案件数【推定】 |
| 新規受注数 | 対象月に登録（受注）された案件数【推定】 |
| 全体稼働率 | `全スタッフ総稼働時間 ÷ 全スタッフ標準稼働時間合計` |

- 売上の認識基準（請求ベース / 完了ベース / 発生ベース）はレポート整合の要。中村確認事項。

### 4.9 顧客別集計（顧客別レポート、月別）
| 項目 | 計算式 |
|---|---|
| 案件数 | 当該顧客の案件数（対象月に稼働があったもの）【推定】 |
| 売上合計 | 当該顧客の請求金額合計 |
| 工数合計 | 当該顧客案件の作業時間合計 |
| 粗利益 / 粗利益率 | `売上 − 原価` / `粗利益 ÷ 売上` |
| 収益性評価 | 粗利益率に応じ 高 / 中 / 低 / 赤字 |

収益性評価の閾値【推定】:
| 条件 | 評価 |
|---|---|
| 粗利益率 ≥ 50% | 高 🟢 |
| 30% ≤ 粗利益率 < 50% | 中 |
| 0% ≤ 粗利益率 < 30% | 低 🟡 |
| 粗利益率 < 0% | 赤字 🔴 |

### 4.10 見積精度分析（完了案件対象）
| 項目 | 計算式 |
|---|---|
| 見積工数 | 案件の見積工数 |
| 実績工数 | 案件の実績工数（時間） |
| 乖離率 | `(実績工数 − 見積工数) ÷ 見積工数` |
| 精度評価 | 乖離率の絶対値に応じ 高 / 中 / 低 |

精度評価・改善提案ロジック（マニュアル 4-10）:
| 乖離率 | 評価 | 改善提案 |
|---|---|---|
| `|乖離率| ≤ 10%` 【推定】 | 高 🟢 | 見積精度良好 |
| `10% < |乖離率| ≤ 20%` 【推定】 | 中 | 微調整を検討 |
| 乖離率 > +20% | 低 🔴 | 見積が甘い → 次回は工数を増やす |
| 乖離率 < −20% | 低 🔴 | 見積が過大 → 次回は工数を減らす |

- 案件種別ごとの平均乖離率を集計し、種別単位の傾向（例:「相続税申告は平均1.3倍」）を提示する。

### 4.11 稼働予測（今月・来月）
| 項目 | 計算式 |
|---|---|
| 月間稼働可能時間 | スタッフマスタの月間標準稼働時間 |
| 確定案件の工数 | 当該スタッフが主担当/割当の進行中・確定案件の残見積工数【推定】 |
| 予測稼働時間 | 実績（経過分）+ 残予測（確定案件の残工数）【推定】 |
| 予測稼働率 | `予測稼働時間 ÷ 月間稼働可能時間` |
| 空き時間 | `月間稼働可能時間 − 予測稼働時間` |
| アラート | 過負荷 / 適正 / 余裕 |

稼働予測アラート閾値【推定】（スタッフ別レポートに準拠）:
| 条件 | アラート | 色 |
|---|---|---|
| 予測稼働率 ≥ 100% | 過負荷 | 🔴 赤 |
| 70% ≤ 予測稼働率 < 100% | 適正 | 🟢 緑 |
| 予測稼働率 < 70% | 余裕 | 🟡 黄 |

- 予測アルゴリズムの厳密化（残工数の按分方法、複数担当案件の配分）は中村確認事項。MVP では「確定案件の残見積工数を主担当へ全量計上」する単純モデルを採用候補とする。

---

## 5. 画面別要件

各画面について **［表示要素］／［操作］／［データ］／［バリデーション］／［自動処理］** を定義する。

---

### 5-1. ダッシュボード（#1）

**［表示要素］**
- 進行中の案件数 / 全案件数
- 今月の総稼働時間
- 予算超過・警告アラートが出ている案件の一覧（案件名・消化率・色バッジ）
- 直近6ヶ月の月次サマリー（売上・粗利益等の推移グラフ：Chart.js）

**［操作］**
- ログイン後の初期表示画面。
- アラート一覧の案件をクリック → 案件管理の該当案件編集へ遷移【推定】。
- 月次サマリーグラフのホバーで値表示。

**［データ］** 案件集計シート、月次サマリーシート（集計済みデータを参照）。

**［バリデーション］** なし（参照専用）。

**［自動処理］**
- 集計シートから読み取り表示（重い再計算はしない＝表示の高速化）。
- 「今月」はサーバ日付基準で判定。

---

### 5-2. 工数入力（#2）

4つのタブ構成: **個別入力 / 一括入力 / タイマー / 履歴**。

#### (A) 個別入力タブ
**［表示要素・入力項目］**
| 項目 | 種別 | 必須 | 既定値 | 備考 |
|---|---|---|---|---|
| 記録日 | 日付 | ○ | 今日 | 過去日入力可（入力漏れ対応） |
| スタッフ | 選択 | ○ | （ログインユーザーは無いので任意既定）【推定】 | スタッフマスタ参照 |
| 案件 | 選択 | ○ | — | 進行中案件を優先表示 |
| 作業項目 | 選択 | ○ | — | 作業種類のマスタ【推定：固定リスト or マスタ化要確認】 |
| 開始時刻 | 時刻 | △ | — | 終了時刻とセットで分換算 |
| 終了時刻 | 時刻 | △ | — | |
| 作業時間（分） | 数値 | △ | — | 時刻入力の代替。直接分入力可 |
| 請求区分 | 選択 | ○ | 請求対象 | 請求対象 / 内部 / サービス |
| 作業内容 | テキスト | 任意 | — | メモ |

**［操作］** 「+ 新規」→ 入力 →「保存」。

**［バリデーション］**
- 記録日・スタッフ・案件・作業項目・請求区分は必須。
- 作業時間は「開始/終了時刻ペア」または「作業時間（分）直接入力」のいずれかが必須（両方空はエラー）。
- 時刻ペア入力時: 終了時刻 > 開始時刻。
- 作業時間（分）は最小入力単位（設定値、既定15分）の倍数であること【推定・要確認】。
- 作業時間（分）は正の整数。上限【推定: 1日24時間=1440分以内 等】。

**［自動処理］**
- 作業時間（時間換算）= 分 ÷ 60。
- 原価金額・請求金額をスタッフ単価から自動計算（4.1）。
- KOSU-ID 採番（排他制御）。
- 保存後、関連案件の消化率・収益が次回集計に反映（即時 or バッチ：API_DESIGN.md）。

#### (B) 一括入力タブ
**［表示要素・操作］**
- スタッフ・記録日を1つ選択し、複数行の作業（案件・作業項目・時間・請求区分・作業内容）をまとめて入力。
- 「一括登録」で全行を一括保存。
**［バリデーション］** 個別入力に準ずる。各行ごとに検証し、エラー行を明示。空行はスキップ。
**［自動処理］** 各行で KOSU-ID 採番・原価/請求金額計算。一括処理は LockService で一括ロック。

#### (C) タイマータブ
**［表示要素］**
- 案件・作業項目の選択。
- 計測表示（経過時間 HH:mm:ss）、計測中インジケータ。
- 「開始」「一時停止」「停止（保存）」ボタン。
**［操作］** 案件・作業項目を選択 →「開始」→（必要に応じ一時停止/再開）→「停止」で経過時間を作業時間として工数レコード化。
**［バリデーション］** 開始前に案件・作業項目必須。停止時に作業時間が最小単位未満なら確認【推定】。
**［自動処理］**
- 経過時間を分に丸め（最小入力単位で丸め）て作業時間に設定。
- 開始/終了時刻を自動記録。
- 停止時に個別入力と同じ保存フロー（原価/請求金額計算、ID採番）。
- ブラウザを閉じても計測状態を復元できるよう、計測開始時刻を localStorage 等に保持【推定・UI_DESIGN.md】。

#### (D) 履歴タブ
**［表示要素］** 過去の工数記録一覧（記録日・スタッフ・案件・作業項目・時間・請求区分・作業内容）。
**［操作］** スタッフ / 案件 / 日付範囲で絞り込み。行クリックで編集、削除可。
**［バリデーション］** 編集時は個別入力と同じ。
**［自動処理］** 編集・削除時に原価/請求金額を再計算。集計への反映。

---

### 5-3. 案件管理（#3）

**［表示要素］**
- 案件一覧（案件名・顧客名・案件種別・ステータス・契約金額・見積工数・主担当・納期・消化率/アラート）。
- ステータス絞り込み（既定: 進行中）。

**［入力項目（新規/編集）］**
| 項目 | 必須 | 選択肢/備考 |
|---|---|---|
| 案件名 | ○ | 例「○○株式会社 決算申告」 |
| 顧客 | ○ | 顧客マスタ参照 |
| 案件種別 | 推奨 | 案件種別マスタ参照 |
| 契約形態 | 推奨 | スポット / 顧問 / タイムチャージ / 固定 |
| 主担当 | 推奨 | スタッフマスタ参照 |
| 契約金額 | 推奨 | 円 |
| 見積工数 | 推奨 | 時間 |
| 納期・期限 | 推奨 | 日付 |
| ステータス | ○ | 見積中 / 進行中 / 完了 / 請求済 / 入金済 / 中止 |

**［操作］** 「+ 新規」で登録。一覧の案件名クリックで編集。ステータスでフィルタ。

**［バリデーション］**
- 案件名・顧客は必須。
- 契約金額・見積工数は0以上の数値。
- 納期は日付形式。
- ステータス遷移の整合【推定】（例: 入金済→見積中 への逆行は警告）。

**［自動処理］**
- PJT-ID 採番。
- 契約金額・見積工数が登録されると消化率・粗利益が集計対象になる。
- 案件種別選択時、標準工数・標準単価を初期値として提案【推定】。

---

### 5-4. 顧客マスタ（#4）

**［表示要素］** 顧客一覧（顧客名・カナ・区分・業種・ランク・ステータス・担当者 等）。

**［入力項目］**
| 項目 | 必須 | 選択肢/備考 |
|---|---|---|
| 顧客名 | ○ | |
| カナ | 任意 | 並べ替え用 |
| 法人/個人区分 | 任意 | 法人 / 個人 |
| 業種 | 任意 | |
| 代表者名 | 任意 | |
| 担当者名 | 任意 | |
| 電話番号 | 任意 | |
| メールアドレス | 任意 | 形式チェック |
| 住所 | 任意 | |
| 顧客ランク | 任意 | A / B / C |
| 支払条件 | 任意 | 翌月末 / 翌々月末 など |
| 請求締日 | 任意 | 末日 / 15日 / 20日 / 25日 |
| ステータス | ○ | 取引中 / 休止 / 解約 |

**［操作］** 「+ 新規」で登録。行クリックで編集。

**［バリデーション］** 顧客名必須。メール形式・電話番号形式チェック【推定】。

**［自動処理］** CUS-ID 採番。

---

### 5-5. スタッフマスタ（#5）

**［表示要素］** スタッフ一覧（氏名・役職・職種・原価単価・請求単価・標準稼働時間・稼働率目標 等）。

**［入力項目］**
| 項目 | 必須 | 選択肢/備考 |
|---|---|---|
| 氏名 | ○ | |
| 役職 | 任意 | 代表 / パートナー / マネージャー / スタッフ / アシスタント |
| 職種 | 任意 | 税理士 / 公認会計士 / エンジニア など |
| 標準時給単価（原価） | 推奨 | 円/時。収益計算の基礎 |
| 標準請求単価（売価） | 推奨 | 円/時。収益計算の基礎 |
| 月間標準稼働時間 | 推奨 | 既定160h |
| 稼働率目標 | 推奨 | 既定0.80（80%） |

**［操作］** 「+ 新規」で登録。行クリックで編集。

**［バリデーション］**
- 氏名必須。
- 単価は0以上の数値。請求単価 ≥ 原価単価を推奨（下回る場合は警告）【推定】。
- 稼働率目標は 0 < 値 ≤ 1（または0〜100%）。

**［自動処理］** STF-ID 採番。単価は工数計算の参照元。

---

### 5-6. 案件種別マスタ（#6）

**［表示要素］** 種別一覧（種別名・カテゴリ大・標準工数・標準単価・難易度）。

**［入力項目］**
| 項目 | 必須 | 選択肢/備考 |
|---|---|---|
| 種別名 | ○ | 例: 法人決算申告 / 個人確定申告 / 顧問契約 / 相続税申告 |
| カテゴリ大 | 任意 | 税務 / 法務 / コンサル など |
| 標準工数（時間） | 推奨 | 見積精度分析の基準 |
| 標準単価 | 推奨 | 標準的な請求額 |
| 難易度 | 任意 | 1〜5 |

**［操作］** 「+ 新規」で登録。行クリックで編集。

**［バリデーション］** 種別名必須。標準工数・標準単価は0以上。難易度は1〜5の整数。

**［自動処理］** KND-ID 採番。見積精度分析で種別別傾向の集計に使用。

---

### 5-7. 経費管理（#7）

**［表示要素］** 経費一覧（発生日・スタッフ・案件・経費種別・金額・請求区分・領収書・承認状態・内容）。

**［入力項目］**
| 項目 | 必須 | 選択肢/備考 |
|---|---|---|
| 発生日 | ○ | 日付 |
| スタッフ | ○ | 経費を使ったスタッフ |
| 案件 | ○ | 関連案件 |
| 経費種別 | ○ | 交通費 / 宿泊費 / 印紙代 / 登録免許税 / 外注費 / 通信費 / 消耗品費 / その他 |
| 金額 | ○ | 円 |
| 請求区分 | ○ | 実費請求 / 含む / 自社負担 |
| 内容 | 任意 | |
| 領収書 | 任意 | 有 / 無 |
| 承認状態 | ○ | 申請中 / 承認 / 却下（既定: 申請中） |

**［操作］** 「+ 新規」で登録。行クリックで編集。

**［バリデーション］** 発生日・スタッフ・案件・経費種別・金額・請求区分必須。金額は正の数値。

**［自動処理］**
- KEIHI-ID 採番。
- 「実費請求」かつ「承認」の経費は請求書自動計算の実費金額に算入（4.6/4.7）。
- 「含む」「自社負担」は案件原価に算入（4.6・要確認）。

---

### 5-8. 請求管理（#8）

**［表示要素］** 請求書一覧（請求番号・顧客・案件・請求日・請求期間・金額・消費税・合計・ステータス・支払期限）。

**［入力項目（新規請求）］**
| 項目 | 必須 | 備考 |
|---|---|---|
| 顧客 | ○ | |
| 案件 | ○ | （複数案件対応可否は要確認【推定: 1請求=1案件】） |
| 請求日 | ○ | 請求番号の YYYYMM 基準 |
| 請求期間 FROM / TO | ○ | 自動計算の集計範囲 |
| 基本報酬 | 自動/手動 | 4.7 |
| タイムチャージ金額 | 自動/手動 | 4.7 |
| 実費金額 | 自動/手動 | 4.7 |
| 消費税 | 自動 | 設定税率 |
| 合計請求額 | 自動 | |
| 支払期限 | ○ | 顧客の支払条件から初期値提案【推定】 |
| ステータス | ○ | 下書き / 発行済 / 入金済 / 一部入金 / 遅延 |

**［操作］** 「+ 新規請求」→ 顧客・案件・請求日・期間設定 →「自動計算」→ 必要に応じ調整 → 支払期限・ステータス設定 →「保存」。

**［バリデーション］**
- 顧客・案件・請求日・請求期間・ステータス必須。
- 請求期間 FROM ≤ TO。
- 金額は0以上。

**［自動処理］**
- INV-YYYYMM-連番 採番（月内リセット、排他制御）。
- 自動計算で基本報酬/タイムチャージ/実費を算出（4.7）。
- 消費税・合計を自動計算。
- 支払期限超過かつ未入金 → ステータス「遅延」自動判定【推定・バッチ or 表示時判定】。

---

### 5-9. 案件別レポート（#9）

**［表示要素］** 案件名・顧客名・契約金額・実績工数・工数消化率・総原価（人件費+経費）・粗利益・粗利益率・予算消化アラート。
**［操作］** 一覧表示。フィルタ/ソート（顧客・ステータス・粗利率等）【推定】。
**［データ］** 案件集計シート（集計済み）。
**［自動処理］** 4.2/4.3/4.4 の色分け適用。集計シート参照（再計算は設定の手動集計 or バッチ）。

---

### 5-10. スタッフ別レポート（#10）

**［表示要素］** 氏名・役職・総稼働時間・請求対象時間・稼働率・稼働率達成度・担当案件数・売上貢献額。
**［操作］** 右上の月選択で対象月変更。
**［データ］** スタッフ集計シート（月別、集計済み）。
**［自動処理］** 4.5 の色分け（稼働率 ≥100% 赤 / <70% 黄）。

---

### 5-11. 月次レポート（#11）

**［表示要素］** 売上合計・前月比・総原価・粗利益・粗利益率・完了案件数・新規受注数・全体稼働率。月推移グラフ（Chart.js）。
**［操作］** 対象月/期間選択【推定】。
**［データ］** 月次サマリーシート。
**［自動処理］** 4.8 の計算。前月比算出。

---

### 5-12. 顧客別レポート（#12）

**［表示要素］** 顧客名・案件数・売上合計・工数合計・粗利益・粗利益率・顧客ランク・収益性評価（高/中/低/赤字）。
**［操作］** 月選択。
**［データ］** 顧客集計シート（月別）。
**［自動処理］** 4.9 の評価・色分け。

---

### 5-13. 見積精度分析（#13）

**［表示要素］** 案件名・案件種別・見積工数・実績工数・乖離率・精度評価（高/中/低）・改善提案。種別別の平均乖離率サマリー。
**［操作］** 完了案件を対象に表示。案件種別でフィルタ【推定】。
**［データ］** 案件集計シート（完了案件）＋案件種別マスタ。
**［自動処理］** 4.10 の乖離率・評価・提案生成。

---

### 5-14. 稼働予測（#14）

**［表示要素］** 対象年月・スタッフ名・月間稼働可能時間・確定案件の工数・予測稼働時間・予測稼働率・空き時間・アラート（過負荷/適正/余裕）。
**［操作］** 今月・来月の切替。
**［データ］** スタッフマスタ＋進行中/確定案件＋工数実績。
**［自動処理］** 4.11 の予測計算・アラート判定。

---

### 5-15. 設定（#15）

**［表示要素・操作（手動集計ボタン）］**
- 案件別集計を実行 → 全進行中案件の工数・収益を再集計。
- 月次集計を実行 → 先月の月次サマリーを再集計。
- スタッフ別集計を実行 → 先月のスタッフ稼働を再集計。
- 見積精度分析を実行 → 完了案件の見積精度を再分析。

**［スプレッドシート側設定（設定_システム設定シート）］**
| 設定項目 | 既定値 | 用途 |
|---|---|---|
| 消費税率 | 0.10 | 請求書の消費税計算 |
| 最小入力単位（分） | 15 | 工数入力の最小単位 |
| 各種アラート閾値 | 警告80% / 超過100% 等 | アラート判定基準 |
| 請求番号プレフィックス | INV | 請求番号の先頭 |
| アラート通知先メール | （任意） | 予算超過時の通知先 |

**［自動処理］** 各集計ボタンは対応するバッチ集計関数を呼び、集計シートを更新（生データは変更しない）。LockService で排他。

---

## 6. データ実体一覧（SHEETS_SCHEMA.md への橋渡し）

本書から導かれる主なデータ実体（詳細列定義は SHEETS_SCHEMA.md）。

**マスタ系（生データ）**
- 顧客（CUS）
- スタッフ（STF）
- 案件種別（KND）
- 案件（PJT）
- 作業項目（マスタ化要否は要確認）

**トランザクション系（生データ）**
- 工数（KOSU）
- 経費（KEIHI）
- 請求書（INV）／請求明細（要確認: 明細を別シート化するか）

**集計系（派生データ・生データは更新しない）**
- 案件集計
- スタッフ集計（月別）
- 月次サマリー
- 顧客集計（月別）
- 見積精度（完了案件）
- 稼働予測

**設定系**
- システム設定

リレーション（1対多・多対多）の明示は SHEETS_SCHEMA.md のリレーション図で行う。代表的な関係:
- 顧客 1 — N 案件
- 案件種別 1 — N 案件
- スタッフ 1 — N 工数 / N 経費 / N 案件（主担当）
- 案件 1 — N 工数 / N 経費 / N 請求書【1請求=1案件想定】
- 案件 N — N スタッフ（工数を通じた実績ベースの多対多）

---

## 7. 非機能要件

| 区分 | 要件 |
|---|---|
| 性能 | 工数レコード 年間1万行超・複数年蓄積に耐える。レポートは集計シート参照で高速表示。 |
| 同時実行 | 複数ユーザー同時書き込みを LockService で排他。採番重複ゼロ。 |
| 拡張性 | DataRepository 層を必ず経由し、Supabase 移行時は Repository 実装の差し替えのみで本体ロジック不変。 |
| 保守性 | コードはコメント多めで引き継ぎ可能。ID命名規則統一。シート設計は正規化しSQLへマッピング容易。 |
| 移植性 | API は REST スタイル（`?action=`）。JSON 構造は Supabase Edge Function 互換。 |
| UI/UX | 日本語UI・日本語エラー。レスポンシブ（PC/スマホ）。色の意味統一（赤/黄/緑）。HYPHEN ロゴ・色は差し替え可能。 |
| 可用性 | スプレッドシートの自動保存・バージョン管理に依拠。定期コピーを運用推奨。 |

---

## 8. 中村レビュー確認事項（オープン論点）

設計を確定する前に判断が必要な【推定】項目を集約する。

1. **作業項目**: 固定リストか、マスタ化（マスタ画面なし＝設定/種別に内包）か。
2. **単価スナップショット**: 工数レコードへ単価を保存し過去金額を固定するか（推奨）／常にマスタ参照か。
3. **経費の原価算入範囲**: 「含む」「自社負担」を案件原価に算入するか（4.6）。
4. **削除方針**: 工数・経費の削除は物理削除か論理削除（削除フラグ）か（推奨: 論理削除）。
5. **売上認識基準**: 月次/顧客別レポートの売上は 請求ベース / 完了ベース / 発生ベース のどれか。
6. **請求と案件の関係**: 1請求=1案件か、複数案件まとめ請求を許すか。請求明細シートの要否。
7. **契約形態と基本報酬**: タイムチャージ契約時の基本報酬の扱い（0とし工数のみ請求 等）。
8. **担当案件数の定義**: 主担当基準か、工数入力した案件のユニーク数か。
9. **稼働予測アルゴリズム**: 残工数の按分・複数担当案件の配分方法。MVPは単純モデル可か。
10. **見積精度の評価閾値**: 高/中/低の境界（本書推定: ±10% / ±20%）。
11. **収益性評価の閾値**: 顧客別「高/中/低/赤字」の境界（本書推定）。
12. **アラートメール通知**: MVP で実装するか将来対応か。
13. **集計の更新タイミング**: 書き込み時の即時集計か、バッチ/手動集計のみか（性能とのトレードオフ）。

---

## 9. 用語集

| 用語 | 定義 |
|---|---|
| 工数 | 案件に費やした作業時間（分/時間）。 |
| 消化率 | 実績工数 ÷ 見積工数。 |
| 原価単価 | スタッフ1時間あたりの社内コスト（標準時給単価）。 |
| 請求単価 | スタッフ1時間あたりの顧客請求額（標準請求単価）。 |
| 粗利益 | 契約金額 − 総原価。 |
| 稼働率 | 稼働時間 ÷ 標準稼働時間。 |
| 稼働率達成度 | 稼働率 ÷ 目標稼働率。 |
| 請求区分（工数） | 請求対象 / 内部 / サービス。 |
| 請求区分（経費） | 実費請求 / 含む / 自社負担。 |
| タイムチャージ | 工数 × 請求単価による従量請求。 |

---

（以上 Step 1。中村のフィードバック確認後、Step 2: SHEETS_SCHEMA.md に進む。）
